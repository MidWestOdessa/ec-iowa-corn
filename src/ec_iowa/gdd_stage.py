"""Logistic crop-stage estimator: pct = 100 / (1 + exp(-k·(GDD - GDD50))).

Parameters per stage live in config.GDD_STAGE_PARAMS. See handoff §3.3.

Implementation pending.
"""
from __future__ import annotations
